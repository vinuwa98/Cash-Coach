<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Existing Group 1 -->
    <group android:id="@+id/group1">
        <item android:id="@+id/dashboard"
            android:title="Dashboard"
            android:icon="@drawable/dashboard"/>
        <item android:id="@+id/stats"
            android:title="Stats"
            android:icon="@drawable/stats"/>
        <item android:id="@+id/income"
            android:title="Income"
            android:icon="@drawable/income"/>
        <item android:id="@+id/expense"
            android:title="Expense"
            android:icon="@drawable/expense"/>
    </group>

    <!-- Existing Group 3 -->
    <group android:id="@+id/group3">
        <item android:id="@+id/account"
            android:title="Account"
            android:icon="@drawable/account"/>
    </group>

    <!-- New Group: Sensor -->
    <group android:id="@+id/group_sensor">
        <item android:id="@+id/sensor1"
            android:title="Sensor"
            android:icon="@drawable/sensor"/>
    </group>

    <!-- New Group: Google Map -->
    <group android:id="@+id/group_google_map">
        <item android:id="@+id/google_map1"
            android:title="Google Map"
            android:icon="@drawable/map"/>
    </group>

    <!-- Existing Group 2 -->
    <group android:id="@+id/group2">
        <item android:id="@+id/logout"
            android:title="Logout"
            android:icon="@drawable/logout"/>
    </group>


</menu>
